import { SignUp } from "@/components/sign-up";
import { View } from "react-native";

const Auth = () => {
  return (
    <View className="flex-1 bg-zinc-900">
      <SignUp />
    </View>
  );
};

export default Auth;
