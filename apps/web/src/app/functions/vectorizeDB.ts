"use server";
import type { wardrobeItemInterface } from "../api/gemini-analyze/route";

export const vectorizeDB = async (input: wardrobeItemInterface) => {

    console.log("Vectorizing DB with input:", input);
};